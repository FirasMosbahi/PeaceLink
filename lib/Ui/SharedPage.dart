import 'package:flutter/material.dart';

class sharedPage extends StatelessWidget {
  const sharedPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("shared ideas");
  }
}
